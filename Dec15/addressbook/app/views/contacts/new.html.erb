<h1>New Contact</h1>

<%= simple_form_for(@contact, :url => create_contact_path) do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :email %>
  <%= f.input :contact_number %>
  <%= f.submit "Save Contact" %>
<% end %>
